# 提词器 镜像同步机制 详解

> 适用版本：v1.09  
> 涉及文件：`mirror_sync_mixin.py`、`mirror_window.py`、`playback_mixin.py`、`prompter_page.py`、`prompter.html`  
> 技术栈：PyQt6 + PyQt6-WebEngine（Chromium内核） + JavaScript

---

## 一、整体架构

```
┌─────────────── PrompterPage ───────────────────────┐
│                                                    │
│  ┌─ PlaybackMixin （播放/滚动/调速）               │
│  │   _tick()         16ms 播放帧                   │
│  │   _tick_scroll()  16ms 键盘滚动帧               │
│  │   _reset_scroll() 重置滚动位置                  │
│  └────────────────────────────────────────────────│
│                                                    │
│  ┌─ MirrorSyncMixin （镜像同步）                   │
│  │   _tick_sync_mirror()   16ms 定时读取主屏状态   │
│  │   _on_sync_position()   回调：推送到镜像窗口    │
│  │   _sync_mirror_content() 重建镜像HTML           │
│  │   _open_mirror()        打开镜像窗口            │
│  │   _close_mirror()       关闭镜像窗口            │
│  └────────────────────────────────────────────────│
│                                                    │
│  QWebEngineView (主屏)          MirrorWindow (镜像) │
│  ┌──────────────┐               ┌──────────────┐   │
│  │ prompter.html │               │ setHtml()     │   │
│  │  .content     │               │ .flip-wrapper │   │
│  │  #rl (引导框) │               │  #rl          │   │
│  └──────────────┘               └──────────────┘   │
└────────────────────────────────────────────────────┘
```

三个核心窗口都是独立的顶层 QMainWindow/QWidget：

| 窗口 | 类 | 说明 |
|------|-----|------|
| 主窗口 | `MainWindow` | QStackedWidget 容器，切换首页/编辑器/提词器 |
| 镜像窗口 | `MirrorWindow` | 独立 QMainWindow，用于提词器硬件反射输出 |
| 控制面板 | `ControlPanel` | 独立 QWidget(Tool)，浮动参数调节 |

---

## 二、数据结构

### 2.1 PrompterPage 中的关键属性

```python
# 滚动相关
self._scroll_position = 0.0       # 当前真实滚动位置（px）
self._scroll_height = 1           # 文档总可滚动高度
self._accumulated_scroll = 0.0    # 播放累积滚动（用于完成检测）
self._pixels_per_second = 0.0     # 当前播放速度（px/s）

# 镜像相关
self._mirror_scale = 1.0          # 镜像缩放比 = mirror_width / main_width
self._mirror_reading_line_y = 0.0 # 镜像窗口的引导框 Y 坐标
self._mirror_window = None        # MirrorWindow 实例
self._is_mirror_open = False      # 镜像窗口是否打开
self._sync_version = 0            # 版本号，用于过滤过期回调

# 引导框
self._reading_line_y = 0          # 主屏引导框 Y 坐标（px）
self._reading_line_visible = True # 引导框是否可见
self._reading_line_opacity = 0.8  # 透明度（0.0-1.0）

# 翻转
self._horizontal_flip = True      # 水平翻转（默认开，用于反射镜）
self._vertical_flip = False       # 垂直翻转（默认关）

# 定时器
self._timer = QTimer(16ms)        # 播放帧定时器 → _tick()
self._scroll_timer = QTimer(16ms) # 键盘滚动定时器 → _tick_scroll()
self._sync_timer = QTimer(16ms)   # 镜像同步定时器 → _tick_sync_mirror()
self._resync_debounce_timer       # 防抖定时器（300ms），尺寸变化后重建HTML
```

### 2.2 MirrorWindow 中的关键属性

```python
self._pending_scroll_y = 0.0      # 待写入的滚动位置
self._pending_rl_y = 0.0          # 待写入的引导框 Y
self._content_html = ""           # 当前 HTML 内容（缓存）
self._hflip = True                # 水平翻转
self._vflip = False               # 垂直翻转
self._reading_line_visible = True # 引导框可见性
self._reading_line_opacity = 0.8  # 引导框透明度
```

---

## 三、镜像同步核心原理

### 3.1 两条同步路径

| 路径 | 触发条件 | 方法 | 频率 |
|------|----------|------|------|
| **完整重建** | 字号/行距/边距变化、翻转切换、重新进入 | `_sync_mirror_content()` | 低频（参数变化时） |
| **增量同步** | 播放中、键盘滚动中 | `_tick_sync_mirror()` | 高频（每16ms） |

### 3.2 路径1：完整重建 HTML

```python
def _sync_mirror_content(self, keep_scroll: bool = True):
    # ① 重新计算缩放比
    main_w = self._view.width()
    mirror_w = self._mirror_window.view_width()
    self._mirror_scale = mirror_w / main_w

    # ② 用缩放比重新生成 HTML
    html = self._build_html(manuscript.content, scale=self._mirror_scale, vflip=self._vertical_flip)

    # ③ 镜像窗口加载新 HTML + 初始滚动位置
    self._mirror_window.set_content(html, scroll_y, mirror_rl_y)
```

**触发场景**：字号/行距/边距滑块拖动时，因为 HTML 模板的 `__FS__`、`__LH__`、`__PX__` 参数变化，需要重新渲染。

### 3.3 路径2：增量同步（16ms 定时器）— **核心高频路径**

```
每 16ms 执行一次：
  _tick_sync_mirror()
    → 主屏 JS 读取 [pageYOffset, readingLineTop, rlHeight]
    → _on_sync_position() 回调
        → 所有值 × _mirror_scale 缩放
        → mirror_window.sync_scroll(scaled_scroll_y)    ← JS: window.scrollTo(0, y)
        → mirror_window.set_reading_line(scaled_rl_y, scaled_rl_h) ← JS: rl.style.top/h = ...
```

**关键设计决策**：
- **只读主屏，推到镜像**：不直接让镜像跟随主屏的 JS 事件
- **版本号过滤**：每次读取递增 `_sync_version`，回调时对比版本号，丢弃过期结果
- **乘缩放比**：所有像素值（scrollY、rlY、rlH）都乘以 `_mirror_scale = mirror_width / main_width`

---

## 四、缩放比计算

```python
# mirror_sync_mixin.py:59
self._mirror_scale = mirror_w / main_w

# mirror_sync_mixin.py:77  
self._mirror_scale = mirror_w / main_w  # resize 时也会更新
```

**什么时候计算**：
1. 打开镜像时（`_open_mirror` → `_sync_mirror_content`）
2. 镜像窗口 resize 时（`_on_mirror_resized` → `_update_mirror_scale`）
3. 主窗口 resize 时（`PrompterPage.resizeEvent` → `_update_mirror_scale`）

**为什么用宽度比**：字号是 px 值，不同分辨率下宽度比例 = 字号缩放比例。

---

## 五、翻转（Flip）实现

### 5.1 CSS Transform 方案

镜像窗口的 HTML 注入 `.flip-wrapper` 包裹层：

```python
# mirror_window.py:21-30
_FLIP_CSS = (
    '<style>'
    '.flip-wrapper{{'
    'transform:scale({sx},{sy});'     # sx=-1 水平翻转, sy=-1 垂直翻转
    'transform-origin:50% 50%;'       # 以中心点为轴
    'width:100%;min-height:100vh;'
    '}}'
    '#rl{{pointer-events:none !important;cursor:default !important;}}'  # 禁止拖拽
    '</style>'
)
```

### 5.2 垂直翻转时的特殊处理

垂直翻转后 Y 轴颠倒，滚动和引导框坐标需要转换：

```python
# 滚动（mirror_window.py:81-84）
if self._vflip:
    maxY = scrollHeight - innerHeight
    window.scrollTo(0, maxY - scroll_y)  # 翻转后的坐标
else:
    window.scrollTo(0, scroll_y)

# 引导框（mirror_window.py:98-99）
if self._vflip:
    rl.style.top = (innerHeight - y - h) + "px"  # 翻转后的 Y
else:
    rl.style.top = y + "px"
```

### 5.3 引导框与翻转层的分离

这是**最关键的设计**。`.flip-wrapper` 用 CSS `transform` 包裹 `.content`，但 `#rl` 是 `<body>` 的直接子元素，**不在 `.flip-wrapper` 内部**：

```python
# mirror_window.py:126-127（加载 HTML 时的注入逻辑）
html = re.sub(r'(<body[^>]*>)', r'\1<div class="flip-wrapper">', html)
# html = re.sub(r'(<div\s+id="rl"[^>]*>)', r'</div>\1', html)  
# ↑ 这行是关键：#rl 被拿到 .flip-wrapper 外面
```

最终 DOM 结构：
```html
<body>
  <div class="flip-wrapper">   <!-- transform 只作用于这里 -->
    <div class="content">...</div>
  </div>
  <div id="rl"></div>           <!-- 不受 transform 影响，position:fixed -->
</body>
```

**为什么这样设计**：引导框是 `position: fixed`，如果被包含在 `.flip-wrapper` 的 `transform` 中，`fixed` 定位会相对于 transform 容器而非真实视口，导致位置错乱。保持 `#rl` 在 body 层级，它的 `fixed` 定位始终相对于浏览器视口。

---

## 六、JS 同步细节

### 6.1 主屏读取 JS（每16ms执行一次）

```javascript
// mirror_sync_mixin.py:186
[
    window.pageYOffset,                                              // 当前滚动位置
    window.getReadingLineTop ? window.getReadingLineTop() : (wiH/3), // 引导框 Y
    (document.getElementById('rl')||{}).offsetHeight || 0             // 引导框高度
]
```

### 6.2 镜像写入 JS（每16ms执行两次）

```javascript
// mirror_window.py:87-88（滚动同步）
window.scrollTo(0, scroll_y);

// mirror_window.py:101-103（引导框同步）
rl.style.top = y + "px";
rl.style.height = h + "px";
rl.style.display = "block" | "none";
```

### 6.3 引导框拖拽（prompter.html:62-85）

```javascript
var rl = document.getElementById('rl');
var dragging = false, startY = 0, startTop = 0;
window._readingLineTop = window.innerHeight / 3;  // 初始为 1/3 视口高度

rl.addEventListener('mousedown', function(e) {
    dragging = true;
    startY = e.clientY;
    startTop = parseInt(rl.style.top) || (window.innerHeight / 3);
});

document.addEventListener('mousemove', function(e) {
    if (!dragging) return;
    var newTop = Math.max(0, Math.min(
        window.innerHeight - rl.offsetHeight,
        startTop + e.clientY - startY
    ));
    rl.style.top = newTop + 'px';
    window._readingLineTop = newTop;  // 更新全局变量
});

// _tick_sync_mirror 通过 window.getReadingLineTop() 读取此值
```

---

## 七、定时器生命周期

```python
# prompter_page.py:__init__
self._sync_timer = QTimer(16ms)       # 16ms 精确定时器
self._sync_timer.timeout.connect(self._tick_sync_mirror)

self._resync_debounce_timer = QTimer()
self._resync_debounce_timer.setSingleShot(True)
self._resync_debounce_timer.setInterval(300)
self._resync_debounce_timer.timeout.connect(lambda: self._sync_mirror_if_open())
```

| 事件 | sync_timer | resync_timer |
|------|------------|--------------|
| 打开镜像 | `_start_sync_timer()` 启动 | - |
| 关闭镜像 | `_stop_sync_timer()` 停止 | - |
| hideEvent（页面隐藏） | 停止 | 停止 |
| showEvent（页面显示） | `_start_sync_timer()` 重新启动 | - |
| 主窗口 resize | - | `_resync_debounce_timer.start()` 延迟 300ms 重建 HTML |
| 镜像窗口 resize | - | 同上 |

---

## 八、性能考量

### 8.1 当前每16ms的操作

```
主屏: 1次 runJavaScript(JS读取数组) → 异步回调
回调中: 
    1次 mirror_window.sync_scroll()    → JS: window.scrollTo()
    1次 mirror_window.set_reading_line() → JS: rl.style.top/height/display
```

**总计每16ms：3次 JS 交互**（1读 + 2写）

### 8.2 潜在瓶颈

1. **runJavaScript 的异步开销**：每次调用都是 IPC 到 Chromium 渲染进程，有固有延迟
2. **低配电脑 16ms 可能丢帧**：如果 JS 回调延迟 > 16ms，版本号过期，该帧被丢弃
3. **垂直翻转的数学计算**：`scrollTo(0, maxY - scroll_y)` 每次需读 `scrollHeight`

### 8.3 优化建议（如需要）

1. 合并两次镜像写入为一次 JS 调用
2. 在 JS 端做节流（throttle），减少 PyQt→Chromium IPC 次数
3. 降低同步频率（16ms → 30ms）如果人眼感知不到差异
4. 使用 `QWebChannel` 替代 `runJavaScript` 的反复调用（双向通信更高效但实现更复杂）

---

## 九、多显示器处理

```python
# mirror_sync_mixin.py:101-124
def _position_mirror_on_secondary(self):
    screens = QApplication.instance().screens()  # 获取所有屏幕
    primary = QApplication.instance().primaryScreen()

    if len(screens) < 2:     # 只有1个显示器 → 不处理
        return
    if len(screens) > 2:     # 超过2个 → 弹提示手动调整
        QMessageBox.information(...)
        return

    for screen in screens:
        if screen is not primary:                 # 找到非主屏 = 副屏
            geo = screen.availableGeometry()
            mirror.move(
                geo.x() + (geo.w - mirror.w) // 2,  # 水平居中
                geo.y() + (geo.h - mirror.h) // 2,  # 垂直居中
            )
            return
```

**主屏判定**：`primaryScreen()` 返回鼠标/窗口所在的那个屏幕。

---

## 十、数据流完整时序图

```
用户操作              主屏                     镜像同步                   镜像屏
───────              ────                     ────────                   ────
打开镜像 ───────────→ _open_mirror()
                      ├─ showNormal()
                      ├─ move到副屏
                      ├─ _sync_mirror_content()
                      │  ├─ 计算 _mirror_scale
                      │  ├─ _build_html(scale, vflip)
                      │  └─ mirror.set_content() ──────────→ setHtml() + scrollTo()
                      └─ _start_sync_timer()  ← 16ms 循环开始

播放中(每16ms) ─────→ _tick()
                      └─ scrollBy(delta)

                      _tick_sync_mirror()
                      └─ runJavaScript(读)
                        返回 [scrollY, rlY, rlH]
                        _on_sync_position()
                        ├─ sync_scroll(y×scale)  ──────────→ window.scrollTo()
                        └─ set_reading_line(...) ──────────→ rl.style.top/h

按↑↓键 ──────────→ _tick_scroll()
                      └─ scrollBy(delta)

                      （同步定时器不受影响，继续16ms读取推送）

关闭镜像 ───────────→ _close_mirror()
                      └─ _stop_sync_timer()
                          mirror.close()
```

---

## 十二、已知问题与根因分析

### 12.1 翻转切换后持续不同步

**现象**：水平/垂直翻转切换后，镜像屏的滚动进度与主屏出现偏差，且不会自动纠正，持续累积。

**触发路径**：

```
用户点击"水平翻转"按钮
  → _on_horizontal_flip_toggled(enabled)
    → set_flip(hflip, vflip, rebuild=False)       # ① 更新 _hflip/_vflip 标志，不重建 HTML
    → _sync_mirror_content(keep_scroll=True)       # ② 重建 HTML + 恢复滚动
```

**根因**：`_sync_mirror_content` 重建 HTML（`setHtml`）是**异步**的——Chromium 需要时间加载新页面。但 `_sync_timer`（16ms）在此期间**仍在运行**，继续用 `_mirror_scale` 将主屏坐标推送到镜像窗口。此时镜像窗口处于旧 HTML → 新 HTML 的过渡态，会出现：

1. `setHtml` 尚未完成加载 → 旧的 `scrollTo()` 对新页面无效
2. 新页面 `_on_page_loaded` 回调出发时，`_pending_scroll_y` 写入初始位置
3. 但 16ms 定时器在此之后继续推送位置，覆盖了初始位置
4. 如果翻转恰好改变了 Y 轴方向（垂直翻转），`_on_sync_position` 中的 `scroll_y * scale` 仍按旧逻辑计算，导致偏移

**关键代码位置**：
- `mirror_sync_mixin.py:150-162`：翻转处理 → `_sync_mirror_content()`
- `mirror_sync_mixin.py:54-66`：`_sync_mirror_content()` 内部 `set_content()`
- `mirror_window.py:63-67`：`set_content()` → `_load_html()` → `setHtml()`（异步）
- `mirror_window.py:134-144`：`_on_page_loaded()`（异步回调写入初始滚动）
- `mirror_sync_mixin.py:177-188`：`_tick_sync_mirror()` 每 16ms 继续推送

**为什么偏差会持续**：翻转后如果出现初始偏移，后续每 16ms `_tick_sync_mirror` 读取的是主屏坐标（正确），但镜像屏因初始偏移已经错位。由于 `sync_scroll` 用绝对坐标 `scrollTo()` 写入，而不是增量 `scrollBy()`，如果镜像屏的 `scrollHeight` 与主屏不同（见 12.2），同一个 `scroll_y × scale` 会落在镜像屏的不同文本位置，偏差永远无法自动纠正。

### 12.2 不同分辨率下英文导致的掉行问题（核心问题）

**现象**：主屏和镜像屏分辨率不同时，**开头和结尾同步但中间不同步**。纯中文文本缩放精度很高，几乎完美对齐。但当文本含有英文单词或字母时，会出现"掉行"——镜像屏比主屏多显示一行或少显示一行，导致滚动位置错位。

**根因**：**中文是等宽方块字（黑体加粗后每个字占宽高度一致），缩放后折行点几乎不变**。但英文单词是不可分割的整体，不同长度的单词在字号缩放取整后，累加的像素宽度可能恰好越过行宽阈值，导致在镜像屏上多折一行或少折一行。

**原理推演**：

```
主屏 1920px 宽，字号 120px，每行可容纳约 16 个中文字：

  中文（等宽方块，完美对齐）：
  主屏: ┌──────────────────────────────────┐
         │ 这是一段很长的提词器文本内容用来 │ ← 16字/行
         │ 测试不同分辨率下的折行效果看看是 │ ← 16字/行
         │ 否会发生掉行现象                 │ ← 9字/行
         └──────────────────────────────────┘

  镜像屏 1280px 宽，字号 80px，每行可容纳约 16 个中文字：
        ┌─────────────────────────┐
         │ 这是一段很长的提词器文本 │ ← 16字/行 ✓ 行数一致
         │ 内容用来测试不同分辨率下 │ ← 16字/行 ✓ 行数一致
         │ 的折行效果看看是否会发生 │ ← 16字/行 ✓ 行数一致
         │ 掉行现象                 │ ← 9字/行 ✓ 行数一致
         └─────────────────────────┘

  → 行数完全一致，scrollHeight 成精确比例，完美同步。

  中英混排（问题场景）：
  主屏: ┌──────────────────────────────────┐
         │ Hello everyone welcome to the   │ ← 英文单词整体占用宽度
         │ teleprompter software test      │
         │ 这是一段提词器文本              │
         └──────────────────────────────────┘

  镜像屏（字号 × scale 后恰好少 1-2px，导致最后一个单词被挤到下一行）：
        ┌─────────────────────────┐
         │ Hello everyone welcome  │ ← 主屏这行有 "to the"，镜像屏 "to the" 掉到下一行
         │ to the teleprompter     │ ← 多出一行！
         │ software test           │
         │ 这是一段提词器文本      │
         └─────────────────────────┘

  → 主屏 3 行，镜像屏 4 行。scrollHeight 不同，同一像素位置对应不同文本。
```

**关键数学**：

```
主屏每行能容纳的英文字符宽度 ≈ 1920 / (120 × 0.6) ≈ 26.7 个字母
镜像屏每行 ≈ 1280 / (80 × 0.6) ≈ 26.7 个字母

理论值相同，但因为：
1. 字号 × scale 后 int() 取整（prompter_page.py:162 fs = int(self._font_size * scale)）
2. 每个字符的渲染宽度在 sub-pixel 精度下可能有 0.5-1px 偏差
3. 英文单词不可分割（默认 white-space: normal 时单词整体换行）
4. 一行中累积的 sub-pixel 偏差 ≥ 1 个单词的宽度 → 触发额外的折行
```

**总结**：掉行的根本原因是**字号缩放取整后 sub-pixel 精度损失 + 英文单词不可分割**的组合效应。纯中文因为每个字都是独立可分割的矩形，sub-pixel 误差在行尾刚好能被最后一个字吸收（中文字可以任意位置换行），所以不掉行。

**为什么百分比同步无法解决**：百分比同步只是让两边滚动进度数字一致，但两屏的 DOM 布局（行数）仍然不同，同一百分比位置对应的文本内容依然不一致。用户的真实需求是**两屏显示的内容完全一致**。

**正确解决方向**：不让镜像屏做字号缩放，而是让镜像屏**以相同的 CSS 参数渲染出与主屏完全一致的 DOM 布局**，再整体缩放输出。

| 方案 | 原理 | 改动范围 |
|------|------|----------|
| **CSS transform 整体缩放** | 镜像屏 `_build_html(scale=1.0)` 不做字号缩放，HTML 与主屏完全一致（折行相同）。往 HTML 中注入 `transform: scale(mirror_scale)` 对 `.flip-wrapper` 做纯绘制缩放。`_tick_sync_mirror` 推送到镜像时 `scroll_y` 不乘 `_mirror_scale`，引导框 Y 也不乘。 | mirror_sync_mixin、_build_html、_tick_sync_mirror、mirror_window.set_content |

**CSS transform 方案的核心逻辑**：
```
主屏: QWebEngineView(1920px 宽) 渲染 text at 120px → DOM 布局有 N 行
镜像: QWebEngineView(1280px 宽) 渲染 text at 120px → DOM 布局有 N 行（完全相同！）
      └─ .flip-wrapper{ transform:scale(0.667) } → 视觉缩放到 1280px

同步: scroll_y_on_mirror = scroll_y_on_main  （不乘 scale，因为 DOM 相同）
      rl_y_on_mirror = rl_y_on_main            （不乘 scale）
```

**注意事项**：
- `transform: scale()` 是纯绘制缩放，不改变 DOM 的 scrollHeight/offsetHeight，所以 `scrollTo` 的坐标轴仍然是原始 DOM 尺寸
- 引导框 `#rl` 是 position:fixed 且在 `.flip-wrapper` 外部，缩放不影响它的视口定位
- `.flip-wrapper` 缩放后内容会溢出 body → 需要 body 设置 `overflow: hidden`，在 JS 中手动控制 `.flip-wrapper` 的滚动

---

## 十三、常见问题排查

| 问题 | 可能原因 | 排查点 |
|------|----------|--------|
| 镜像不滚动 | `_sync_timer` 未启动 | 检查 `_is_mirror_open` 是否为 True |
| 镜像位置跳变 | `_mirror_scale` 未更新或为0 | 检查 `_update_mirror_scale()` 是否被调用 |
| 引导框位置不对 | 翻转时 Y 坐标计算错误 | 检查 `_vflip` 时 `set_reading_line` 的公式 |
| 性能卡顿 | 16ms 回调来不及 | 检查 `_sync_version` 是否大量丢帧 |
| 镜像窗口不动 | MirrorWindow 的 resized 信号打断 | 检查 `ASPECT_RATIO` 限制是否导致 resize 死循环 |
| 翻转后不同步 | 同步定时器与 HTML 重建有竞态（见第十二章） | 检查 `_sync_mirror_content` 与 `_tick_sync_mirror` 的时序 |
| 中间不同步 | 文字折行导致 scrollHeight 不成线性比例（见第十二章） | 检查两屏字号 × scale 后的实际 scrollHeight 差异 |
